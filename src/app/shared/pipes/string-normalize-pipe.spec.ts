import { StringNormalizePipe } from './string-normalize-pipe';

describe('StringNormalizePipe', () => {
  it('create an instance', () => {
    const pipe = new StringNormalizePipe();
    expect(pipe).toBeTruthy();
  });
});
